# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10  # Allow time for module downloads on first run; cached runs complete in ~30s
    steps:
    - uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.24'
        cache: true

    - name: Build
      run: go build -v ./...

    - name: Test
      timeout-minutes: 10  # Step-level timeout (slightly higher than go test timeout)
      run: go test -v -coverprofile=coverage.txt -covermode=atomic -timeout=8m -parallel=2 -skip='^Fuzz' ./...
      env:
        GOMAXPROCS: 2  # Match GitHub Actions runner CPU count to avoid resource contention

    - name: Test Fuzz Corpus
      timeout-minutes: 5  # Fuzz corpus tests should be quick
      run: go test -v -run='^Fuzz' -timeout=2m ./parser
      env:
        GOMAXPROCS: 1  # Limit to single CPU to prevent resource exhaustion

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
