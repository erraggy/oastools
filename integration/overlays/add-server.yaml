overlay: 1.0.0
info:
  title: Add production server
  version: 1.0.0
actions:
  - target: "$.servers"
    description: Add a production server to the servers list
    update:
      url: https://api.production.example.com
      description: Production server
