openapi: 3.0.0
info:
  title: Malformed Path API
  version: "1.0.0"
paths:
  /users/{{id}}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
